import os

class ExportService:

    def export_csv(self, df, table_name):

        os.makedirs("outputs", exist_ok=True)

        file_path = f"outputs/{table_name}.csv"

        df.to_csv(file_path, index=False)

        return file_path